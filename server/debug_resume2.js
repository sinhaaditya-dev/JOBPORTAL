require('dotenv').config(); const mongoose = require('mongoose'); const User = require('./src/models/User'); const axios = require('axios'); const pdf = require('pdf-parse'); const { analyzeResume } = require('./src/services/geminiService'); async function debug() { await mongoose.connect(process.env.MONGODB_URI); const user = await User.findOne({ 'resume.url': { $ne: '' } }).sort({updatedAt: -1}); if (!user) { console.log('No user with resume'); return; } console.log('Found user:', user.email); try { const response = await axios.get(user.resume.url, { responseType: 'arraybuffer' }); console.log('Downloaded PDF, size:', response.data.length); const pdfData = await pdf(response.data); console.log('Extracted text length:', pdfData.text.length); const report = await analyzeResume(pdfData.text); console.log('Report generated successfully:', report); } catch(e) { console.error('Error during process:', e); } mongoose.disconnect(); } debug();
